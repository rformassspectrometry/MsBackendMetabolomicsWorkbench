% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/Metabolomics_Workbench_functions.R
\name{MetabolomicsWorkbench-utils}
\alias{MetabolomicsWorkbench-utils}
\alias{mwb_list_files}
\alias{mwb_rest_request}
\alias{mwb_ftp_list_files}
\alias{mwb_ftp_download}
\alias{mwb_sync_data_files}
\alias{mwb_cached_data_files}
\alias{mwb_delete_cache}
\title{Utility functions for the Metabolomics Workbench repository}
\usage{
mwb_list_files(x = character(), pattern = NULL)

mwb_rest_request(
  mwbId = character(),
  outputItem = character(),
  outputFormat = "json"
)

mwb_ftp_list_files(mwbId = character(), pattern = "*")

mwb_ftp_download(
  mwbId = character(),
  pattern = "*",
  path = "./",
  overwrite = FALSE
)

mwb_sync_data_files(
  mwbId = character(),
  pattern = "mzML$|mzml$|CDF$|cdf$|mzXML$",
  fileName = character(),
  ftp_zip = FALSE
)

mwb_cached_data_files(
  mwbId = character(),
  pattern = "*",
  fileName = character()
)

mwb_delete_cache(mwbId = character())
}
\arguments{
\item{x}{\code{character(1)} with the ID of the MBW data set (usually
starting with a \emph{ST} followed by a number).}

\item{pattern}{for \code{mwb_list_files()}, \code{mwb_sync_data_files()},
\code{mwb_cached_data_files()}, \code{mwb_ftp_list_files} and \code{mwb_ftp_download}:
\code{character(1)} defining a pattern to filter the file names, such as
\code{pattern = "mzML$"} to retrieve the file names of all files of the data
set (i.e., files with extension \code{"mzML"}). This parameter is passed to
the \code{\link[=grepl]{grepl()}} function.}

\item{mwbId}{\code{character(1)} with the ID of a single Metabolomics
Workbench data set/experiment.}

\item{outputItem}{for \code{mwb_rest_request()}: \code{character(1)} defining the
metadata to retrieve from Metabolomics Workbench. To get more information
about the possible output visit the webpage
\href{https://metabolomicsworkbench.org/tools/mw_rest.php}{MBW REST API}.}

\item{outputFormat}{for \code{mwb_rest_request()}: \code{character(1)} defining the
output format of the metadata. The supported output are \code{json} and \code{txt}.}

\item{path}{for \code{mwb_ftp_download()}: optional \code{character} defining the
directory where download the files.}

\item{overwrite}{for \code{mwb_ftp_download()}: \code{logical(1)} whether
existing files should be overwritten. Defaults to \code{FALSE}, in which
case files that already exist in \code{path} are skipped.}

\item{fileName}{for \code{mwb_sync_data_files()} and \code{mwb_cached_data_files()}:
optional \code{character} defining the names of specific data files of a data
set that should be downloaded and cached.}

\item{ftp_zip}{for \code{mwb_sync_data_files()}: \code{logical(1)} download the
complete zip of the experiment from the FTP server. Defaults to \code{FALSE},
in which case the files are downloaded singularly via POST request.}
}
\value{
\itemize{
\item For \code{mwb_list_files()}: \code{data.frame} with columns \code{zip_file} and
\code{sample_file} containing, respectively, the archive name and the relative
file within that archive
\item For \code{mwb_rest_request()}: \code{character(1)} with the raw REST API response
body, formatted as JSON or plain text depending on \code{outputFormat}.
\item For \code{mwb_sync_data_files()} and \code{mwb_cached_data_files()}: a
\code{data.frame} with the MWB ID, the name(s) and remote and
local file names of the synchronized data files.
\item For \code{mwb_ftp_list_files}: \code{character} with the files in FTP server for a
specific ID.
}
}
\description{
Utility functions to interact with the Metabolomics Workbench (MWB)
repository, including listing, downloading, caching, and querying data
files and study metadata.
\itemize{
\item \code{mwb_cached_data_files()}: lists locally cached data files from
Metabolomics Workbench. Since this function evaluates only local content
it does not require an internet connection. With the default parameters all
available data files are listed. The parameters can be used to restrict the
lookup.
\item \code{mwb_list_files()}: returns the available files for the specified
Metabolomics Workbench data set by submitting a POST request to the
Metabolomics Workbench archive contents endpoint. The function returns a
\code{data.frame} with columns \code{"zip_file"} and \code{"sample_file"} containing the
archive name and the file name within that archive.
Parameter \code{pattern} allows to filter the results by matching against the
\code{"sample_file"} column. This function requires an active internet
connection.
\item \code{mwb_rest_request()}: queries the Metabolomics Workbench REST API for
a given study ID and output item (e.g. \code{summary}, \code{factors}).
Returns the raw response as a \code{character} string in the format specified
by \code{outputFormat} (\code{json} or \code{txt}). This function requires an
active internet connection.
\item \code{mwb_ftp_list_files()}: queries the Metabolomics Workbench FTP server for a
given experiment ID and returns the related files. Parameter \code{pattern}
allows to filter the results. In contrast to \code{mwb_list_files()}, this
function lists only the files on the FTP server (like the zip file of the
experiment), while \code{mwb_list_files()} lists the files contained within the
zip file. Other files may also be present on the FTP server. This function
requires an active internet connection.
\item \code{mwb_ftp_download()}: download files from Metabolomics Workbench FTP
server for a given experiment ID. Use \code{pattern} to filter files by name
using a regular expression (by default all files are downloaded). Use
\code{path} to set the destination directory for downloaded files. Only files
listed by \code{mwb_ftp_list_files()} can be downloaded.
\item \code{mwb_sync_data_files()}: synchronize data files of a specified
MWB data set eventually downloading and locally caching them.
Parameter \code{fileName} allows to specify names of selected data files to
sync.
\item \code{mwb_delete_cache()}: removes all local content for the mwb
data set with ID \code{mwbId}. This will delete eventually present
locally cached data files for the specified data set. This does not
change any other data eventually present in the local \code{BiocFileCache}.
}
}
\details{
Metabolomics Workbench provides metadata through a
\href{https://metabolomicsworkbench.org/tools/mw_rest.php}{REST API}. MS data
files can be obtained in two ways:
\enumerate{
\item Downloading the full \emph{zip} archive from the
\href{ftp://www.metabolomicsworkbench.org/Studies/}{FTP server}. A POST
request to the
\href{https://metabolomicsworkbench.org/data/show_archive_contents_link.php}{MWB archive page}
gets the correct \emph{zip} archive name for a MWB ID. The archive contains
all files of the experiment, which may include also unneeded files. If
only a subset of files is needed, the second option is more efficient.
\item Download individual files using a two-step POST-based procedure: query the
\href{https://metabolomicsworkbench.org/data/show_archive_contents_link.php}{MWB archive page}
to get exact file names. Then, download each file via
\href{https://metabolomicsworkbench.org/data/file_extract_7z.php}{POST request}.
}
}
\examples{

## Retrieve available files for the data set ST002115
mwb_list_files("ST002115")

## Retrieve the available .mzML files.
A1_mzMLfiles <- mwb_list_files("ST000016", pattern = "A1")
A1_mzMLfiles

## Query the REST API for a study summary in JSON format
mwb_rest_request("ST002115", outputItem = "summary")

## List zip file of the data set ST002115
mwb_ftp_list_files("ST002115")

## Download the file with: `mwb_ftp_download("ST002115", path = tempdir())`
}
\author{
Gabriele Tomè, Johannes Rainer, Philippine Louail
}
